import { BuscadorPedidosPipe } from './buscador-pedidos.pipe';

describe('BuscadorPedidosPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorPedidosPipe();
    expect(pipe).toBeTruthy();
  });
});
